@model IEnumerable<CPWebApp.Models.RECURSOS>

@{
    ViewBag.Title = "RECURSOS";
}
<div class="row mt-4 mb-4 align-items-center">
    <div class="col-10">
        <h2>RECURSOS</h2>
    </div>
    <div class="col-2 text-sm-right d-none d-lg-block">
        @Html.ActionLink("Criar novo", "Create", null, new { @class = "btn btn-success" })
    </div>
    <div class="col-2 text-sm-right d-lg-none">
        <a href="@Url.Action("Create")" class="text-success"><i class="material-icons" style="font-size: 36px;">add</i></a>
    </div>
</div>


<div class="table-responsive">
    <table id="table" class="table table-striped">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PESSOAS.nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SISTEMAS.nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TIPO_RECURSO.titulo)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>

@foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayTextFor(modelItem => item.PESSOAS.nome)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.SISTEMAS.nome)
                </td>
                <td>
                    @Html.DisplayTextFor(modelItem => item.TIPO_RECURSO.titulo)
                </td>
                <td class="text-center">
                    <a href="@Url.Action("Edit", new { id=item.id })" class="text-primary"><i class="material-icons">edit</i></a>
                    <a href="@Url.Action("Details", new { id=item.id })" class="text-primary"><i class="material-icons">info</i></a>
                    <a href="@Url.Action("Delete", new { id=item.id })" class="text-danger"><i class="material-icons">delete</i></a>
                </td>
            </tr>
}
        </tbody> 
    </table>
</div>

@section scripts {

    <script type="text/javascript">

    $(document).ready(function(){

        $('#table').DataTable({
            "order": [],
            "language": {
                "search": "Pesquisar:",
                "lengthMenu": "Exibir _MENU_ registros por página",
                "zeroRecords": "Nenhum registro encontrado",
                "info": "Exibindo página _PAGE_ de _PAGES_",
                "infoEmpty": "Nenhum registro encontrado",
                "infoFiltered": "(filtro de _MAX_ registros)",
                "paginate": {
                    "first": "Primeira",
                    "last": "Última",
                    "next": "Próxima",
                    "previous": "Anterior"
                }
            }
        });

    });

    </script>

}

